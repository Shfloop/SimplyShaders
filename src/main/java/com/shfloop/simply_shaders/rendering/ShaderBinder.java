package com.shfloop.simply_shaders.rendering;

public class ShaderBinder {
    //needs to go through each shader i want to use and add bind the shader than bind the texture uniforms
    //to the textures defined in the frame buffer object
}
