package com.shfloop.simply_shaders.pack_loading;

//hold information for a shader pass like what drawbuffers size, buffers to mip map, uniforms,


public class ShaderDirectives {
    private int[] drawBuffers;




    public int[] getDrawBuffers() {
        return drawBuffers;
    }
}
